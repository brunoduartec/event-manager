/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  output: 'export',
  // appDir não é mais necessário nas versões recentes do Next.js
};

module.exports = nextConfig;
